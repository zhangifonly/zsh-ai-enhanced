# AI 命令专家系统 - 快速参考

## 🚀 启动面板的三种方式

### 方式 1：直接使用Python脚本（✅ 推荐，无需密码）

```bash
# 查看所有专家（面板）
~/Documents/ClaudeCode/zsh/zsh/ai_expert_panel.py list

# 其他命令
~/Documents/ClaudeCode/zsh/zsh/ai_expert_panel.py view <专家ID>
~/Documents/ClaudeCode/zsh/zsh/ai_expert_panel.py edit <专家ID>
~/Documents/ClaudeCode/zsh/zsh/ai_expert_panel.py create
```

### 方式 2：全局命令（需要先创建链接）

**创建链接（只需执行一次，需要密码）：**
```bash
sudo ln -sf ~/Documents/ClaudeCode/zsh/zsh/ai_expert_panel.py /usr/local/bin/ai-expert
sudo ln -sf ~/Documents/ClaudeCode/zsh/zsh/ai_experts/web_query.py /usr/local/bin/ai-query
```

**使用命令：**
```bash
ai-expert list          # 查看所有专家
ai-expert view git      # 查看Git专家
ai-expert edit docker   # 编辑Docker专家
ai-expert create        # 创建新专家

ai-query command docker       # 查询命令用法
ai-query expert claude        # 查询专家工具
ai-query best "Python async"  # 搜索最佳实践
```

### 方式 3：使用别名（需要先 source ~/.izshrc）

```bash
# 重新加载配置
source ~/.izshrc

# 使用别名
experts              # 查看所有专家
expert-view git      # 查看Git专家
expert-edit docker   # 编辑Docker专家
expert-new           # 创建新专家

cmd-help docker      # 查询命令用法
expert-help claude   # 查询专家工具
find-fix "问题描述"  # 查找解决方案
best-practice "主题" # 搜索最佳实践
```

## 📋 所有可用命令

### 专家管理命令

| 命令 | 功能 | 示例 |
|------|------|------|
| `list` | 显示所有专家列表（面板） | `ai-expert list` |
| `view <id>` | 查看专家详细提示词 | `ai-expert view git` |
| `edit <id>` | 编辑专家提示词 | `ai-expert edit docker` |
| `create` | 创建自定义专家 | `ai-expert create` |
| `toggle <id>` | 启用/禁用专家 | `ai-expert toggle python` |
| `help` | 显示帮助信息 | `ai-expert help` |

### 联网查询命令

| 命令 | 功能 | 示例 |
|------|------|------|
| `command <cmd>` | 查询命令用法 | `ai-query command docker` |
| `expert <name>` | 查询专家工具 | `ai-query expert claude` |
| `best <topic>` | 搜索最佳实践 | `ai-query best "Python async"` |
| `solve <problem>` | 查找问题解决方案 | `ai-query solve "npm install失败"` |

## 🎯 常见使用场景

### 场景 1：查看所有可用的专家

```bash
# 显示完整列表面板（14个专家）
ai-expert list
# 或
experts
```

**输出**：彩色面板显示所有专家的名称、描述、状态、触发命令等信息。

### 场景 2：查看某个专家的详细提示词

```bash
# 查看Git专家的完整提示词
ai-expert view git

# 查看Docker专家
expert-view docker

# 查看Claude Code专家
expert-view claude
```

**输出**：显示专家的完整提示词内容（带行号）。

### 场景 3：编辑专家提示词

```bash
# 使用默认编辑器（$EDITOR）编辑Git专家
ai-expert edit git

# 或使用别名
expert-edit git
```

**效果**：打开编辑器，可以修改专家的提示词内容。

### 场景 4：创建自定义专家

```bash
# 启动交互式创建向导
ai-expert create
# 或
expert-new
```

**交互流程**：
```
专家 ID (英文，如 mysql): postgres
专家名称 (如 PostgreSQL 数据库专家): PostgreSQL 数据库专家
简短描述: PostgreSQL 查询和数据库管理
触发命令 (逗号分隔，如 mysql,mycli): psql,postgres

✅ 已创建专家: PostgreSQL 数据库专家
📝 模板文件: ~/.izsh/ai_experts/custom/postgres.prompt

💡 下一步：
   1. 编辑提示词: ai-expert edit postgres
   2. 查看提示词: ai-expert view postgres
```

### 场景 5：查询命令最新用法

```bash
# 查询Docker命令的官方文档和用法
ai-query command docker

# 查询kubectl用法
cmd-help kubectl
```

**输出**：
1. 本地man手册（如果有）
2. --help帮助信息
3. AI联网搜索的最新用法、示例和最佳实践

### 场景 6：查询专家工具的最新功能

```bash
# 查询Claude Code的最新功能
ai-query expert claude

# 查询GitHub Copilot
expert-help copilot
```

**输出**：AI联网搜索的最新功能、文档链接、使用技巧等。

### 场景 7：搜索最佳实践

```bash
# 搜索Python异步编程最佳实践
ai-query best "Python async programming"

# 搜索Docker最佳实践
best-practice "Docker 生产环境部署"
```

**输出**：核心概念、常见模式、性能优化、常见陷阱等。

### 场景 8：查找问题解决方案

```bash
# 查找npm安装失败的解决方案
ai-query solve "npm install 失败 EACCES 权限错误"

# 查找Docker问题
find-fix "Docker 容器无法连接到网络"
```

**输出**：
1. 可能的原因
2. 解决方案和步骤
3. Stack Overflow/GitHub Issues讨论
4. 预防方法

## 🔧 环境变量配置

### 自动加载配置

```bash
# 显示欢迎消息（默认）
export IZSH_SHOW_EXPERT_WELCOME=true

# 隐藏欢迎消息
export IZSH_SHOW_EXPERT_WELCOME=false

# 保持专家上下文（命令执行后不清除）
export IZSH_KEEP_EXPERT_CONTEXT=true

# 查看当前加载的专家
echo $IZSH_CURRENT_EXPERT

# 查看专家提示词
echo $IZSH_EXPERT_PROMPT
```

## 📂 文件路径

```bash
# 配置目录
~/.izsh/ai_experts/

# 配置文件
~/.izsh/ai_experts/experts.json

# 预置专家模板
~/.izsh/ai_experts/templates/*.prompt

# 自定义专家
~/.izsh/ai_experts/custom/*.prompt

# 自动加载脚本
~/.izsh/ai_experts/auto_load_expert.sh

# 管理脚本
~/Documents/ClaudeCode/zsh/zsh/ai_expert_panel.py
~/Documents/ClaudeCode/zsh/zsh/ai_experts/web_query.py
```

## 🆘 故障排查

### 问题 1：命令找不到

**错误**：`ai-expert: command not found`

**解决方法**：
```bash
# 方法1：使用完整路径
~/Documents/ClaudeCode/zsh/zsh/ai_expert_panel.py list

# 方法2：创建全局链接（需要密码）
sudo ln -sf ~/Documents/ClaudeCode/zsh/zsh/ai_expert_panel.py /usr/local/bin/ai-expert
```

### 问题 2：别名不可用

**错误**：`experts: command not found`

**解决方法**：
```bash
# 重新加载配置
source ~/.izshrc

# 检查别名是否存在
alias | grep expert
```

### 问题 3：专家自动加载不工作

**检查步骤**：
```bash
# 1. 检查配置是否加载
grep -A5 "AI 命令专家系统 - 自动加载" ~/.izshrc

# 2. 检查函数是否定义
type auto_load_ai_expert

# 3. 手动测试
source ~/.izsh/ai_experts/auto_load_expert.sh
auto_load_ai_expert "git status"
echo $IZSH_CURRENT_EXPERT
```

### 问题 4：Python脚本没有执行权限

**解决方法**：
```bash
chmod +x ~/Documents/ClaudeCode/zsh/zsh/ai_expert_panel.py
chmod +x ~/Documents/ClaudeCode/zsh/zsh/ai_experts/web_query.py
```

## 💡 实用技巧

### 技巧 1：快速查看专家列表

```bash
# 最简单的方式
experts

# 或创建更短的别名
alias e='ai-expert list'
```

### 技巧 2：结合grep筛选专家

```bash
# 查找包含"数据库"的专家
ai-expert list | grep "数据库"

# 查找所有启用的专家
ai-expert list | grep "✅ 启用"
```

### 技巧 3：批量查看专家提示词

```bash
# 查看所有专家的提示词前30行
for expert in git docker python vim claude; do
    echo "=== $expert ==="
    ai-expert view $expert | head -30
    echo ""
done
```

### 技巧 4：快速编辑专家

```bash
# 在 ~/.bashrc 或 ~/.zshrc 中设置默认编辑器
export EDITOR=vim
# 或
export EDITOR=code  # VS Code
export EDITOR=nano  # Nano编辑器

# 然后直接编辑
expert-edit git
```

## 🎓 下一步

1. **浏览文档**：
   ```bash
   cat ~/.izsh/ai_experts/快速开始.md
   cat ~/.izsh/ai_experts/README.md
   ```

2. **尝试自动加载**：
   ```bash
   source ~/.izshrc
   git status  # 会自动加载Git专家
   docker ps   # 会自动加载Docker专家
   ```

3. **创建自己的专家**：
   ```bash
   expert-new  # 交互式创建
   ```

4. **查询最新用法**：
   ```bash
   cmd-help docker      # 查询Docker命令
   expert-help claude   # 查询Claude Code
   ```

---

**快速开始命令**：
```bash
# 查看所有专家
experts

# 查看Git专家详情
expert-view git

# 查询Docker命令用法
cmd-help docker
```

**享受AI命令专家系统！** 🎉
